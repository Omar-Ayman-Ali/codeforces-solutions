#include <bits/stdc++.h>
using namespace std;

#define fast                     \
    ios::sync_with_stdio(false); \
    cin.tie(NULL);
#define all(x) x.begin(), x.end()
#define rall(x) x.rbegin(), x.rend()

typedef long long ll;
typedef vector<int> vi;

int main()
{
    fast;
    int x[3];
    for (int i = 0; i < 3; i++)
        cin >> x[i];
    int z[3];
    for (int i = 0; i < 3; i++)
        z[i] = x[i] #include<bits / stdc++.h> using namespace std;

#define fast                     \
    ios::sync_with_stdio(false); \
    cin.tie(NULL);
#define all(x) x.begin(), x.end()
#define rall(x) x.rbegin(), x.rend()

    typedef long long ll;
    typedef vector<int> vi;

    int main()
    {
        fast;
        int x[3];
        for (int i = 0; i < 3; i++)
            cin >> x[i];
        int z[3];
        for (int i = 0; i < 3; i++)
            z[i] = x[i];
        sort(x, x + 3);
        for (int i = 0; i < 3; i++)
            cout << x[i] << endl;
        cout << endl;
        for (int i = 0; i < 3; i++)
            cout << z[i] << endl;
    };
    sort(x, x + 3);
    for (int i = 0; i < 3; i++)
        cout << x[i] << endl;
    cout << endl;
    for (int i = 0; i < 3; i++)
        cout << z[i] << endl;
}